/* eslint-disable node/no-unpublished-import */
/* eslint-env jest */
jest.setTimeout(128000);

describe('monitor incoming SMS', () => {
  test('as super admin', async () => {
    console.log('as super admin');
  });
  test('as standard user', async () => {
    console.log('as standard user');
  });
});
